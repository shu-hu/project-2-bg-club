  <% if (!user) { %>
    <link rel="stylesheet" href="/stylesheets/siwg.css">
  <% } %>
</head>
<body>
  <nav>
    <i class="fas fa-dragon" id="logo">BG-CLUB</i>
    <a href="/" <%- title === 'Home' ? 'class="active"' : '' %>>HOME</a>
    <a href="/boardgames/top" <%- title === 'Top' ? 'class="active"' : '' %>>DISCOVER</a>
    <a href="/boardgames/search" <%- title === 'Search' ? 'class="active"' : '' %>>SEARCH</a>
    <a href="/favorite" <%- title === 'Favorite' ? 'class="active"' : '' %>>FAVORITE</a>

    <% if (user) { %>
      <a href="/auth/logout">
        <button type="submit" class="fas fa-sign-out-alt" id="log-out">LOGOUT</button>
      </a>
    <% } else { %>
      <a href="/auth/google" class="siwg-link">
        <button type="submit" class="siwg">
          <img id="siwg-image" alt="Sign in with Google" />
        </button>
      </a>
    <% } %>
  </nav>
